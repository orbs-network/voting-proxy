{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build && node --check snapshot-strategies/voting-proxy/index.js && node --check snapshot-strategies/voting-proxy/proxyScoring.js",
    "test": "forge test && node --test test/*.test.js",
    "coverage": "forge coverage --report summary --exclude-tests && node --experimental-test-coverage --test test/*.test.js"
  }
}
