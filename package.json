{
  "name": "voting-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build && node --check snapshot-strategies/voting-proxy/index.js && node --check snapshot-strategies/voting-proxy/proxyScoring.js && node --check scripts/submit-vote.mjs",
    "test": "forge test && npm run test:strategy",
    "test:strategy": "node --test test/*.test.js",
    "coverage": "forge coverage --report summary && node --experimental-test-coverage --test test/*.test.js"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
